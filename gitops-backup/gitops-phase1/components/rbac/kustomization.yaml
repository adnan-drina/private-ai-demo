apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

metadata:
  name: cross-namespace-rbac
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  - cross-namespace-access.yaml

# RBAC Component for Cross-Namespace Access
# 
# Enables private-ai-demo to access shared services in ai-infrastructure
# 
# Grants:
#   - Service discovery (get, list)
#   - Service proxy access (for direct calls)
# 
# For:
#   - Milvus vector database
#   - Docling document processing


