apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

metadata:
  name: minio
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  - namespace.yaml
  - secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - secret-ref-for-apps.yaml

# MinIO S3-compatible object storage (optional)
# Alternative to PVC storage for models
# Provides S3 API for model storage and retrieval
