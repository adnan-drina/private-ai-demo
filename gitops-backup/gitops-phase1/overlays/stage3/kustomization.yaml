apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: stage3-enterprise-mcp
  annotations:
    description: "Stage 3: Enterprise Agentic AI - ACME LithoOps Agent with MCP Servers"

# Include all Stage 1 + 2 resources
resources:
  - ../stage2  # ← Inherits all Stage 1 + 2 components and patches
  
  # Add Stage 3 Kustomizations
  - ../../components/acme-agent
  - ../../components/mcp-servers/database-mcp
  - ../../components/mcp-servers/kubernetes-mcp
  - ../../components/mcp-servers/slack-mcp

# Stage 3 patches (if needed)
# Currently no additional DSC changes for Stage 3
# All required components are already enabled in Stage 1 + 2

