apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-from-acme-calibration-ops
  namespace: private-ai-demo
  labels:
    app: llama-stack
spec:
  podSelector: {}
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector:
        matchLabels:
          kubernetes.io/metadata.name: acme-calibration-ops

