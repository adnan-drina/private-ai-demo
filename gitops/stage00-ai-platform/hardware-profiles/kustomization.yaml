---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: redhat-ods-applications

resources:
  # New infrastructure API (RHOAI 2.25+)
  - nvidia-l4-1gpu.yaml
  - nvidia-l4-4gpu.yaml
  # Dashboard-compatible API (deprecated but needed for UI)
  - nvidia-l4-1gpu-dashboard.yaml
  - nvidia-l4-4gpu-dashboard.yaml

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/managed-by: argocd
      app.kubernetes.io/instance: stage01-hardware-profiles

