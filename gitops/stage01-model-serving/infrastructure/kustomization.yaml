---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Infrastructure components deployed to redhat-ods-applications
# These resources support the RHOAI dashboard integration

resources:
  # Hardware and Accelerator Profiles for GPU nodes
  - hardware-profiles/nvidia-l4-1gpu.yaml
  - hardware-profiles/nvidia-l4-4gpu.yaml
  - hardware-profiles/nvidia-l4-1gpu-dashboard.yaml
  - hardware-profiles/nvidia-l4-4gpu-dashboard.yaml

# Note: Resources have explicit namespace declarations
# No commonLabels to avoid overriding resource-specific labels

