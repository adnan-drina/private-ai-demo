apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: minio
  annotations:
    config.kubernetes.io/local-config: "true"

resources:
  # namespace.yaml removed - using parent namespace (private-ai-demo)
  # secret.yaml excluded - managed by deploy.sh (not committed to Git)
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - route.yaml
  - secret-ref-for-apps.yaml

# MinIO S3-compatible object storage (optional)
# Alternative to PVC storage for models
# Provides S3 API for model storage and retrieval
