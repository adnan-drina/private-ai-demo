apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# PipelineRuns are declarative but typically triggered manually or by CI/CD
# These are templates that can be applied when needed

resources:
  - pipelinerun-mistral-24b-quantized.yaml
  - pipelinerun-mistral-24b-full.yaml

commonLabels:
  argocd.argoproj.io/instance: stage01-model-serving
  app.kubernetes.io/instance: stage01-pipelineruns
  app.kubernetes.io/managed-by: argocd

# Note: PipelineRuns are immutable once created
# To re-run, delete the old PipelineRun and apply again
# Or use generateName instead of name for auto-generated names

