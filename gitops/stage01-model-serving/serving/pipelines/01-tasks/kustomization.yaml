apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Legacy tasks (kept for reference)
  - task-buildah-build.yaml
  - task-prepare-modelcar.yaml
  - task-mirror-to-quay.yaml
  - task-deploy-vllm.yaml
  
  # v2 tasks (production-ready, works for both quantized and full models)
  - task-build-and-push-to-quay.yaml
  - task-mirror-to-internal.yaml
  - task-register-model.yaml

commonLabels:
  argocd.argoproj.io/instance: stage01-model-serving
  app.kubernetes.io/instance: stage01-pipelines
  app.kubernetes.io/managed-by: argocd

