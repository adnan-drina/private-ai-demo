apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Legacy pipeline (original design, kept for reference)
  - pipeline-model-deployment.yaml
  
  # v2 pipeline (production-ready: handles both quantized and full precision)
  - pipeline-modelcar-v2.yaml

commonLabels:
  argocd.argoproj.io/instance: stage01-model-serving
  app.kubernetes.io/instance: stage01-pipelines
  app.kubernetes.io/managed-by: argocd

