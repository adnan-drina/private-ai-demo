apiVersion: v1
kind: Service
metadata:
  name: slack-mcp
  namespace: private-ai-demo
  labels:
    app: slack-mcp
    component: mcp-server
spec:
  selector:
    app: slack-mcp
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP


