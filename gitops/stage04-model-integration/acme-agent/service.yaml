apiVersion: v1
kind: Service
metadata:
  name: acme-agent
  namespace: private-ai-demo
  labels:
    app: acme-agent
    component: agent
spec:
  selector:
    app: acme-agent
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP


