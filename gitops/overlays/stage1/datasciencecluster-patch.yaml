---
# Stage 1: Enable Model Registry
apiVersion: datasciencecluster.opendatahub.io/v1
kind: DataScienceCluster
metadata:
  name: default-dsc
spec:
  components:
    modelregistry:
      managementState: Managed  # Stage 1: Enable Model Registry for MLOps tracking

