apiVersion: console.openshift.io/v1
kind: ConsolePlugin
metadata:
  name: console-plugin-nvidia-gpu
  namespace: nvidia-gpu-operator
spec:
  displayName: NVIDIA GPU Console Plugin
  backend:
    type: Service
    service:
      name: console-plugin-nvidia-gpu
      namespace: nvidia-gpu-operator
      port: 9443
      basePath: /

