---
apiVersion: v1
kind: Service
metadata:
  name: guidellm-reports
  namespace: private-ai-demo
  labels:
    app: guidellm-reports
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  selector:
    app: guidellm-reports
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

