---
# Template only – hydrated by deploy.sh using values from project .env
apiVersion: v1
kind: Secret
metadata:
  name: guardrails-credentials
  annotations:
    kubernetes.io/service-account.name: guardrails-workload-sa
type: Opaque
stringData:
  openai-api-key: "${GUARDRAILS_OPENAI_API_KEY}"
  huggingface-token: "${HF_TOKEN}"


