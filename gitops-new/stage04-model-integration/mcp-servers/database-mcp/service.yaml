apiVersion: v1
kind: Service
metadata:
  name: database-mcp
  namespace: private-ai-demo
  labels:
    app: database-mcp
    component: mcp-server
spec:
  selector:
    app: database-mcp
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
  type: ClusterIP


