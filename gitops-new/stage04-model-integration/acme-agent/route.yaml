apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: acme-agent
  namespace: private-ai-demo
  labels:
    app: acme-agent
spec:
  to:
    kind: Service
    name: acme-agent
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect


