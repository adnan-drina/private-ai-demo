---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: llama-stack-metrics
  namespace: private-ai-demo
  labels:
    app: llama-stack
spec:
  selector:
    matchLabels:
      app: llama-stack
  podMetricsEndpoints:
  - port: http
    path: /metrics
    interval: 30s

