apiVersion: v1
kind: Service
metadata:
  name: database-mcp
  namespace: private-ai-demo
  labels:
    app: database-mcp
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: database-mcp

