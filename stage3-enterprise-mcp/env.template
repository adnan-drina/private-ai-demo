# ACME LithoOps Agent - Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy this file to .env:
#    cp env.template .env
# 2. Fill in your actual values below
# 3. The .env file is git-ignored for security
# 4. Never commit .env to version control

# ============================================
# Slack Configuration (Optional)
# ============================================
# Get your webhook URL from: https://api.slack.com/apps
# 
# How to create a Slack webhook:
# 1. Go to https://api.slack.com/apps
# 2. Click "Create New App" → "From scratch"
# 3. Name: "ACME LithoOps Agent", select your workspace
# 4. Navigate to "Incoming Webhooks"
# 5. Toggle "Activate Incoming Webhooks" to ON
# 6. Click "Add New Webhook to Workspace"
# 7. Select channels: #acme-litho and #acme-alerts
# 8. Copy the webhook URL and paste below
#
# Example format: https://hooks.slack.com/services/T00000000/B00000000/XXXXXXXXXXXXXXXXXXXX
# 
# ⚠️  OPTIONAL: Leave empty for DEMO MODE (console logging only)
# For real Slack alerts, replace with your webhook URL:
# Note: Channels (#acme-litho, #acme-alerts) are hardcoded in Java code
SLACK_WEBHOOK_URL=

# ============================================
# PostgreSQL Configuration
# ============================================
# Database credentials for equipment database
# These are used by both PostgreSQL and Database MCP
POSTGRES_DB=acme_equipment
POSTGRES_USER=acmeadmin
POSTGRES_PASSWORD=acme_secure_2025

# PostgreSQL connection (internal service name)
POSTGRES_HOST=postgresql.private-ai-demo.svc.cluster.local
POSTGRES_PORT=5432

# ============================================
# OpenShift Configuration
# ============================================
# Namespaces for deployment
PROJECT_PRIVATE_AI=private-ai-demo
PROJECT_ACME_AGENT=acme-calibration-ops

# ============================================
# Optional: Custom vLLM Endpoint
# ============================================
# Override default vLLM endpoint if using custom deployment
# Default: http://mistral-24b-quantized-predictor.private-ai-demo.svc.cluster.local/v1
# VLLM_ENDPOINT=

# ============================================
# Optional: Custom MCP Endpoints
# ============================================
# Override if deploying MCP servers in different namespaces
# DATABASE_MCP_URL=http://database-mcp.private-ai-demo.svc.cluster.local:8080
# SLACK_MCP_URL=http://slack-mcp.private-ai-demo.svc.cluster.local:8080

# ============================================
# Optional: Build Configuration
# ============================================
# Skip tests during Maven build (default: true)
# SKIP_TESTS=true

# Maven options (optional)
# MAVEN_OPTS="-Xmx2g"

