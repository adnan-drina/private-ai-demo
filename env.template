# Private AI Demo - Environment Configuration Template
#
# ════════════════════════════════════════════════════════════════════════
# INSTRUCTIONS FOR SECURE SECRET MANAGEMENT
# ════════════════════════════════════════════════════════════════════════
#
# This template follows Red Hat OpenShift GitOps best practices for managing
# secrets in GitOps workflows. Secrets are NEVER committed to Git.
#
# Setup Steps:
# ────────────────────────────────────────────────────────────────────────
# 1. Copy this template to create your local .env file:
#    cp env.template .env
#
# 2. Edit .env and fill in your actual secret values
#
# 3. Verify .env is git-ignored (it should be by default):
#    git check-ignore .env
#    (should output: .env)
#
# 4. Run stage deploy scripts - they will automatically:
#    - Load variables from project root .env file
#    - Create Kubernetes Secrets imperatively
#    - Deploy GitOps manifests (without secrets)
#
# 5. NEVER commit .env or any file containing real secrets!
#
# ════════════════════════════════════════════════════════════════════════
# Red Hat Recommendations:
# ════════════════════════════════════════════════════════════════════════
#
# Current Approach (Interim):
#   ✅ Secrets in single root .env file (git-ignored)
#   ✅ Imperative secret creation via deploy.sh scripts
#   ✅ GitOps manifests reference secrets by name only
#
# Future Enhancement (Recommended for Production):
#   🎯 External Secrets Operator (ESO)
#   🎯 HashiCorp Vault or AWS Secrets Manager
#   🎯 Secrets Store CSI Driver
#   🎯 Sealed Secrets for GitOps
#
# References:
#   - Red Hat OpenShift GitOps Security Guide
#   - https://docs.redhat.com/en/documentation/red_hat_openshift_gitops/
#   - https://external-secrets.io/
#   - https://ai-on-openshift.io/odh-rhoai/secret-management/
#
# ════════════════════════════════════════════════════════════════════════

# ════════════════════════════════════════════════════════════════════════
# STAGE 1: Model Serving with vLLM - SECRETS
# ════════════════════════════════════════════════════════════════════════

# HuggingFace Token (REQUIRED)
# ───────────────────────────────────────────────────────────────────────
# Get token: https://huggingface.co/settings/tokens
# Create "Read" token for model downloads
HF_TOKEN=

# MinIO Credentials (REQUIRED)
# ───────────────────────────────────────────────────────────────────────
# Object storage for model artifacts and benchmark results
MINIO_ACCESS_KEY=admin
MINIO_SECRET_KEY=<get-from-cluster>

# Quay.io Credentials (REQUIRED for ModelCar Pipeline)
# ───────────────────────────────────────────────────────────────────────
# Get credentials: https://quay.io/ (Account Settings → Generate Encrypted Password)
# Or use Robot Account for automation
QUAY_USERNAME=
QUAY_PASSWORD=
QUAY_ORGANIZATION=

# ════════════════════════════════════════════════════════════════════════
# STAGE 4: MCP Integration - SECRETS (Optional)
# ════════════════════════════════════════════════════════════════════════

# Slack Webhook URL (OPTIONAL)
# ───────────────────────────────────────────────────────────────────────
# Get webhook: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=

# PostgreSQL Credentials (OPTIONAL)
# ───────────────────────────────────────────────────────────────────────
# ACME equipment database credentials
POSTGRES_USER=acme_admin
POSTGRES_PASSWORD=acme-password-change-me
POSTGRES_DATABASE=acme_equipment

# ════════════════════════════════════════════════════════════════════════
# COMMON CONFIGURATION (Non-sensitive)
# ════════════════════════════════════════════════════════════════════════

# Project namespace
PROJECT_NAME=private-ai-demo

# Infrastructure namespace
INFRASTRUCTURE_NAMESPACE=ai-infrastructure
