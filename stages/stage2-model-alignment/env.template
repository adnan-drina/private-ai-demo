# Stage 2: Private Data RAG - Environment Configuration Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env:
#    cp env.template .env
# 2. Fill in your actual values below
# 3. The .env file is git-ignored for security
# 4. Never commit .env to version control

# ============================================
# OpenShift Configuration
# ============================================
# Namespace for RAG deployment
PROJECT_NAME=private-ai-demo

# ============================================
# PostgreSQL Configuration (for pgvector)
# ============================================
# Database credentials for vector store
# Default values are suitable for demo, change for production
POSTGRES_DB=vectordb
POSTGRES_USER=rag_user
POSTGRES_PASSWORD=rag_secure_2025

# PostgreSQL connection (internal service name)
POSTGRES_HOST=postgresql-pgvector.private-ai-demo.svc.cluster.local
POSTGRES_PORT=5432

# ============================================
# vLLM Configuration
# ============================================
# vLLM endpoint from Stage 1
# Default: http://mistral-24b-quantized-predictor.private-ai-demo.svc.cluster.local/v1
VLLM_ENDPOINT=http://mistral-24b-quantized-predictor.private-ai-demo.svc.cluster.local/v1

# Model name for completion
MODEL_NAME=mistralai/Mistral-Nemo-Instruct-2407

# ============================================
# RAG Configuration
# ============================================
# Embedding model (defaults to sentence-transformers/all-MiniLM-L6-v2)
# EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# Chunk size for document splitting
# CHUNK_SIZE=500

# Chunk overlap
# CHUNK_OVERLAP=50

# Number of retrieved documents
# TOP_K=3

# ============================================
# MinIO Configuration (for KFP artifacts)
# ============================================
# MinIO endpoint from Stage 0
MINIO_ENDPOINT=minio.model-storage.svc.cluster.local:9000

# MinIO credentials (get from stage00 secret: minio-root-credentials)
# Run: oc get secret minio-root-credentials -n model-storage -o jsonpath='{.data.accesskey}' | base64 -d
MINIO_ACCESS_KEY=minio
# Run: oc get secret minio-root-credentials -n model-storage -o jsonpath='{.data.secretkey}' | base64 -d
MINIO_SECRET_KEY=minio123

# MinIO bucket for KFP artifacts
MINIO_KFP_BUCKET=kfp-artifacts

# ============================================
# Optional: API Keys for External Services
# ============================================
# OpenAI API key (if using OpenAI embeddings)
# OPENAI_API_KEY=

# HuggingFace API key (if using HuggingFace inference API)
# HUGGINGFACE_API_KEY=

